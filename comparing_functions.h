#include <stdbool.h>

#ifndef COMPARING_FUNCTIONS_H
#define COMPARING_FUNCTIONS_H

bool compare_strs(char* a, char* b);
bool if_same(char* a, char* b);
bool if_space(char* d);

#endif //COMPARING_FUNCTIONS_H